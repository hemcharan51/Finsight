"""FinSight v2.0 — Grid-Native Agentic Analysis for Structured Financial Documents.

A grid in the shape of Hebbia, with FinSight's deterministic core intact:
every question is decomposed into a matrix of documents (rows) × sub-questions
(columns); every cell is answered independently, in parallel, scoped to a single
document; and every number leaves the LLM and goes through a deterministic engine.
"""

__version__ = "2.0.0"
